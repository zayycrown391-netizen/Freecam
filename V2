<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="utf-8" />
<meta name="viewport" content="width=device-width,initial-scale=1">
<title>Freecam Menu</title>
<style>
  @import url('https://fonts.googleapis.com/css2?family=Inter:wght@600;700&display=swap');

  html,body{
    margin:0;
    height:100%;
    background:transparent;
    overflow:hidden;
    user-select:none;
    font-family:'Inter',sans-serif;
    font-weight:600;
    color:#fff;
  }

  body {
    display:flex;
    align-items:center;
    justify-content:center;
    flex-direction:column;
    text-align:center;
    letter-spacing:0.3px;
  }

  .menu {
    opacity:0;
    transform:translateY(20px);
    animation:fadeInUp .6s cubic-bezier(.16,.84,.44,1) forwards;
  }

  @keyframes fadeInUp {
    to { opacity:1; transform:translateY(0); }
  }

  .menu-item {
    font-size:20px;
    margin:8px 0;
    transition:all .18s ease;
    opacity:0.7;
    text-shadow:0 0 8px rgba(0,0,0,0.5);
  }

  .menu-item.active {
    color:#ff4a4a;
    font-size:22px;
    opacity:1;
    text-shadow:0 0 12px rgba(255,74,74,0.7);
    transform:scale(1.04);
  }

  .menu-item.secondary {
    color:#ff7070;
  }

  .menu-item span {
    color:#ff4a4a;
  }

  .hint {
    margin-top:40px;
    font-size:13px;
    color:rgba(255,255,255,0.5);
    letter-spacing:1px;
  }

  /* smooth scroll / selector effect */
  .selector {
    position:absolute;
    width:280px;
    height:34px;
    border-radius:6px;
    background:rgba(255,255,255,0.06);
    box-shadow:0 0 8px rgba(255,255,255,0.1);
    left:50%;
    transform:translateX(-50%);
    transition:all .2s ease;
    z-index:-1;
  }
</style>
</head>
<body>
  <div class="menu">
    <div id="selector" class="selector"></div>
    <div class="menu-item active">- Look-Around -</div>
    <div class="menu-item">Teleport</div>
    <div class="menu-item secondary">Action <span>( M82v2 )</span></div>
    <div class="menu-item secondary">Car Shooter <span>( Cargoplane )</span></div>
    <div class="menu-item">Spawn Angry Ped</div>
    <div class="menu-item">Delete Entity</div>
    <div class="menu-item">Flip Vehicle</div>
    <div class="menu-item">Unlock Vehicle</div>
    <div class="hint">Scroll • Select</div>
  </div>

<script>
  const items = [...document.querySelectorAll('.menu-item')];
  const selector = document.getElementById('selector');
  let index = 0;

  function updateActive(newIndex){
    items[index].classList.remove('active');
    index = (newIndex + items.length) % items.length;
    items[index].classList.add('active');
    const rect = items[index].getBoundingClientRect();
    selector.style.top = (rect.top + window.scrollY - 4) + 'px';
  }

  // Initialize selector position
  window.onload = () => {
    const rect = items[index].getBoundingClientRect();
    selector.style.top = (rect.top + window.scrollY - 4) + 'px';
  }

  // Scroll / key simulation (for visual testing)
  window.addEventListener('wheel', (e)=>{
    if(e.deltaY>0) updateActive(index+1);
    else updateActive(index-1);
  });
</script>
</body>
</html>
