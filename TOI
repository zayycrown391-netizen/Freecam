<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="utf-8" />
<meta name="viewport" content="width=device-width,initial-scale=1" />
<title>Menu</title>
<style>
  @import url('https://fonts.googleapis.com/css2?family=Inter:wght@700;800&display=swap');

  :root{
    --fg:#ffffff;
    --muted:rgba(255,255,255,.70);
    --dash:#0f5e2b;             /* dark green on active/hover */
    --dash-muted:rgba(255,255,255,.35);
    --shadow:rgba(0,0,0,.70);
    --bottom: 9%;               /* lower/higher: tweak this */
  }

  html,body{
    margin:0;height:100%;background:transparent;overflow:hidden;
    user-select:none;color:var(--fg);
    font-family:'Inter',system-ui,Segoe UI,Roboto,Arial;
    font-weight:800;
  }

  /* lower centered stack (no panel) */
  .wrap{
    position:fixed;left:50%;bottom:var(--bottom);
    transform:translateX(-50%);
    pointer-events:auto;
  }

  .list{ position:relative; text-align:center; }

  .item{
    font-size:20px; line-height:30px; letter-spacing:.25px;
    margin:6px 0; opacity:.95; transform:scale(1);
    text-shadow:
      0 0 2px #000, 0 1px 0 #000, 1px 0 0 #000, -1px 0 0 #000,
      0 0 14px var(--shadow);
    transition: transform .16s ease, opacity .16s ease, filter .16s ease;
    position:relative; display:inline-block; padding:0 22px;
  }
  .item span{opacity:.9;}

  /* side dashes (muted by default) */
  .item::before,
  .item::after{
    content:""; position:absolute; top:50%; width:84px; height:0;
    border-top:2px dashed var(--dash-muted);
    transform:translateY(-50%);
    transition: border-color .16s ease, filter .16s ease, width .2s ease;
    filter: drop-shadow(0 2px 4px rgba(0,0,0,.35));
  }
  .item::before{ right:100%; margin-right:10px; }
  .item::after { left:100%;  margin-left:10px;  }

  /* active/hover -> greener dashes + gentle bump */
  .item.active,
  .item:hover{
    transform:scale(1.06);
  }
  .item.active::before, .item.active::after,
  .item:hover::before, .item:hover::after{
    border-color:var(--dash);
    width:96px;
  }

  /* bottom hint + fade */
  .hint{
    margin-top:10px;font-size:12px;color:var(--muted);letter-spacing:1px;
    text-shadow:0 0 10px rgba(0,0,0,.6);
  }
  .fade-bottom{
    position:absolute; left:50%; transform:translateX(-50%);
    width:420px; height:80px; bottom:-60px; pointer-events:none;
    background:linear-gradient(to bottom, rgba(0,0,0,0) 0%, rgba(0,0,0,.78) 100%);
    filter: blur(2px);
  }

  /* gentle pop-in */
  .list.animate-in{ opacity:0; transform:translateY(8px); animation:in .35s ease forwards; }
  @keyframes in{ to{opacity:1; transform:none;} }
</style>
</head>
<body>
  <div class="wrap">
    <div class="list animate-in" id="list">
      <div class="item active">- Look-Around -</div>
      <div class="item">Teleport</div>
      <div class="item">Action <span>( M82v2 )</span></div>
      <div class="item">Car Shooter <span>( Cargoplane )</span></div>
      <div class="item">Spawn Angry Ped</div>
      <div class="item">Delete Entity</div>
      <div class="item">Flip Vehicle</div>
      <div class="item">Unlock Vehicle</div>
      <div class="fade-bottom"></div>
    </div>
    <div class="hint">Scroll • Select</div>
  </div>

<script>
  const items = [...document.querySelectorAll('.item')];
  const list  = document.getElementById('list');
  let idx = 0;

  function setActive(newIdx){
    items[idx].classList.remove('active');
    idx = (newIdx + items.length) % items.length;
    items[idx].classList.add('active');
  }

  // init
  window.addEventListener('load', ()=> setActive(0));

  // mouse wheel (only when cursor is over the list area)
  list.addEventListener('wheel', (e)=>{
    e.preventDefault();
    (e.deltaY > 0) ? setActive(idx + 1) : setActive(idx - 1);
  }, {passive:false});

  // arrow keys (if DUI has focus)
  window.addEventListener('keydown', (e)=>{
    if (e.key === 'ArrowDown') setActive(idx + 1);
    else if (e.key === 'ArrowUp') setActive(idx - 1);
  });

  // hover also highlights (keeps UI feeling responsive)
  items.forEach((el, i)=>{
    el.addEventListener('mouseenter', ()=> setActive(i));
  });
</script>
</body>
</html>
